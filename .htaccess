<IfModule mod_rewrite.c>
    RewriteEngine On
    Options -Indexes

    # Send root requests to /public
    RewriteRule ^$ public/ [L]

    # Send all other requests to /public
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
